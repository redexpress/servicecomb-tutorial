pipeline {
    agent {label 'master'}
    stages {
        stage('Build') {
            steps {
                //运行构建
                shell "echo 'Build'"
            }
        }
        stage('Test') {
            steps {
                //测试
                shell "echo 'Test'"
            }
        }
        stage('Push') {
            steps {
                //发布上线
                shell "echo Push"
            }
        }
    }
}